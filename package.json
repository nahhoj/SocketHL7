{
  "name": "com.co.cnet.socket",
  "version": "1.0.0",
  "description": "Server / client socket tcp ip",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "run": "npm run build && node dist/server/server"
  },
  "author": "Johan Calderon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "fs": "0.0.1-security",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.7",
    "@types/node": "^14.6.0",
    "@types/socket.io": "^2.1.11",
    "typescript": "^3.9.7"
  }
}
